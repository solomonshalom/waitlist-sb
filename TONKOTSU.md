# REPO CONTEXT
This file contains important context about this repo for [Tonkotsu](https://www.tonkotsu.ai) and helps it work faster and generate better code.

## Setup Commands
```bash
# Clone repository
git clone https://github.com/solomonshalom/waitlist.justice.rest
cd waitlist.justice.rest

# Install dependencies (uses npm based on package.json and README)
npm install
```

## Build Commands
```bash
# Build the application
npm run build
```

## Lint Commands
No lint commands are configured in this repository.

## Test Commands
No test commands are configured in this repository.

## Dev Server Commands
```bash
# Start development server
npm run dev

# Preview production build
npm run preview
```

## Additional Notes
- This is a Vite-based frontend project with TypeScript
- Uses PNPM lockfile but README shows npm commands - both work
- No testing framework or linting tools are currently configured
- Project includes CSS/SCSS assets and uses Vite for bundling
- Node.js >= 14.0.0 required